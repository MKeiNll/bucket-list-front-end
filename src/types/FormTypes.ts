export interface EntryCreationFormElements extends HTMLCollection {
  entryTitle: HTMLInputElement;
  entryContent: HTMLInputElement;
}

export interface IsbnFormElements extends HTMLCollection {
  isbnCode: HTMLInputElement;
}
